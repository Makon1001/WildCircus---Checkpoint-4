{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}




{% block body %}

    <h1 class="mb-5 mt-5">Le Wild Circus c'est quoi ? </h1>
    <div class="row">
        <div class="col-12 col-md-6">
            <h2 class="mt-2 mb-2">Une experience Magnifique !</h2>
            <p> Pressure at the galaxy was the devastation of collision course, destroyed to a brave machine. Nanomachines malfunction on love at subspace! </p>
            <h2 class="mt-2 mb-2">Des étoiles plein les yeux !</h2>
            <p>Never trade a parrot. Ah there's nothing like the old booty laughing on the ale. Booty is an evil sail. Where is the shiny mast?</p>
        </div>
        <div class="col-12 col-md-6 mt-2 mb-2">
            <img src="./images/imageTest.jpg" alt="image" style="width: 25rem;">
        </div>
    </div>
    <hr class="mt-5 mb-5"/>
    <div class="parallax"></div>
    <hr class="mt-5 mb-5"/>

    <h1>Toutes nos actualités </h1>

    <div class="row">
    {% for actu in actus %}
            <div class="card col-12 col-md-6 col-lg-4" style="width: 18rem;">
                <img src="./images/actualites/{{ actu.image }}" class="card-img-top" alt="{{ actu.title }}">
                <div class="card-body">
                    <h5 class="card-title">{{ actu.title }}</h5>
                    <h6>{{ actu.subtitle }}</h6>
                    <p class="card-text">{{ actu.content }}</p>
                </div>
            </div>
    {% endfor %}
    </div>
    <hr class="mt-5 mb-5"/>
    <div class="parallax"></div>
    <hr class="mt-5 mb-5"/>

    <h1>Nos spectacles</h1>

    <div class="row">
    {% for spectacle in spectacles %}
        <div class="card col-12 col-md-6 col-lg-4" style="width: 18rem;">
            <img src="./images/spectacles/{{ spectacle.image }}" class="card-img-top" alt="...">
            <div class="card-body">
                <h5 class="card-title">{{ spectacle.name }}</h5>
                <p class="card-text">{{ spectacle.content }}</p>
                <p class="card-text">{{ spectacle.date |date('Y-m-d')}}</p>
                <button type="button" class="btn btn-warning" data-toggle="modal" data-target="#ModalResa" data-whatever="@mdo">Réserver un billet</button>
            </div>
        </div>
    {% endfor %}
</div>
    {%  include "modalResa.html.twig"%}

{% endblock %}
